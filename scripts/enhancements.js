/** * thinkersgk.com — UX enhancements * Zero dependencies. Four features: * 1. Scroll progress bar (thin accent line at viewport top) * 2. CSS spotlight on service cards (cursor-following glow via custom props) * 3. Subtle 3D tilt on cards (perspective transform on mousemove) * 4. Blog art mapping for custom post thumbnails */ (function () { // ── Blog Art Map for Custom Thumbnails ─────────────────────────────── function buildBlogArtMap() { var map = { // Exact post matches (most specific) 'm-trends-2026-japan-business-security-cyber': '../../assets/blog-m-trends-2026-800x500.jpg', 'edge-ai-japan-2026-business-guide': '../../assets/blog-edge-ai-japan-2026-800x500.jpg', 'enterprise-ai-agent-orchestration-2026': '../../assets/blog-enterprise-ai-800x500.jpg', 'ai-attack-evolution-2026-japan': '../../assets/blog-ai-attack-800x500.jpg', // Generic topic matches (fallback) 'cybersecurity': '../../assets/blog-cybersecurity-800x500.jpg', 'ai': '../../assets/blog-ai-800x500.jpg', 'japan': '../../assets/blog-japan-800x500.jpg' }; return map; } function getBlogArtUrl(slug) { var map = buildBlogArtMap(); // First try exact match if (map[slug]) return map[slug]; // Then try prefix match for (var key in map) { if (slug.indexOf(key) === 0) return map[key]; } return null; } function applyBlogArt() { var heroImg = document.querySelector('.blog-post-hero img'); if (!heroImg) return; // Get slug from current page URL var path = window.location.pathname; var match = path.match(/posts\/([^.]+)\.html/); if (!match) return; var slug = match[1]; var artUrl = getBlogArtUrl(slug); if (artUrl) { heroImg.src = artUrl; } } // ── 1. Scroll progress bar ────────────────────────────────────────────────── function initScrollProgress() { var bar = document.createElement('div'); bar.id = 'scroll-progress'; document.body.appendChild(bar); var raf; window.addEventListener('scroll', function () { if (raf) return; raf = requestAnimationFrame(function () { raf = null; var scrolled = document.documentElement.scrollTop || document.body.scrollTop; var total = document.documentElement.scrollHeight - window.innerHeight; bar.style.width = (total > 0 ? (scrolled / total) * 100 : 0) + '%'; }); }, { passive: true }); } // ── 2. Card spotlight (cursor-following glow) ─────────────────────────────── function initCardSpotlight() { var cards = document.querySelectorAll('.card, .service-feature-card, .illustration-card'); cards.forEach(function (card) { card.classList.add('spotlight-card'); card.addEventListener('mousemove', function (e) { var rect = card.getBoundingClientRect(); card.style.setProperty('--mx', (e.clientX - rect.left) + 'px'); card.style.setProperty('--my', (e.clientY - rect.top) + 'px'); }); card.addEventListener('mouseleave', function () { card.style.setProperty('--mx', '-999px'); card.style.setProperty('--my', '-999px'); }); }); } // ── 3. 3D card tilt ───────────────────────────────────────────────────────── function initCardTilt() { var MAX_TILT = 6; // degrees var cards = document.querySelectorAll('.card, .service-feature-card'); cards.forEach(function (card) { card.addEventListener('mousemove', function (e) { var rect = card.getBoundingClientRect(); var cx = rect.left + rect.width / 2; var cy = rect.top + rect.height / 2; var dx = (e.clientX - cx) / (rect.width / 2); var dy = (e.clientY - cy) / (rect.height / 2); card.style.transform = 'perspective(600px) rotateX(' + (-dy * MAX_TILT) + 'deg) rotateY(' + (dx * MAX_TILT) + 'deg) translateZ(4px)'; }); card.addEventListener('mouseleave', function () { card.style.transition = 'transform 0.4s ease'; card.style.transform = ''; setTimeout(function () { card.style.transition = ''; }, 400); }); }); } // ── Boot ───────────────────────────────────────────────────────────────────── document.addEventListener('DOMContentLoaded', function () { initScrollProgress(); initCardSpotlight(); initCardTilt(); applyBlogArt(); }); })();